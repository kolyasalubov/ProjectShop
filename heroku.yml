build:
  docker:
    web: ProjectShop/Dockerfile
run:
  web: gunicorn ProjectShop.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web
#  command:
#    - python manage.py collectstatic --noinput