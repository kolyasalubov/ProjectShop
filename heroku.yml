build:
  docker:
    web: ProjectShop/Dockerfile
#    bot: TelegramBot/Dockerfile
run:
  web: sh -c "python manage.py collectstatic --noinput && gunicorn ProjectShop.wsgi:application --bind 0.0.0.0:$PORT"
#  bot: python main.py
release:
  image: web
  command:
    - python manage.py migrate
